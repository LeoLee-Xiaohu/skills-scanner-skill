# Copy to .env and fill in your values
OPENAI_API_KEY=sk-...

# Optional: override the model used for LLM-as-a-judge (default: gpt-4o)
# SCANNER_LLM_MODEL=gpt-4o

# Optional: disable LLM layer by default for faster local dev
# SCANNER_DEFAULT_LAYERS=pattern,yara,dataflow
